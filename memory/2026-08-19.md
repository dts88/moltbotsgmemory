# 2026-08-19

- Tianshu 要求用 EIA API 整理过去 3 年美国从加拿大、委内瑞拉进口原油量，并生成 Excel。
- 已新增一次性脚本 `scripts/eia-country-crude-imports-excel.mjs`，使用 EIA `/petroleum/move/wimpc/data/`，产品 `EPC0`、流程 `IM0`，国家 `NUS-NCA` / `NUS-NVE`。
- 输出文件：`reports/eia/us-crude-imports-canada-venezuela-2023-08-19_to_2026-08-19.xlsx`。包含官方周度、由周度均值派生的月度、EIA 原始数据和 README。
- 后续 Tianshu 追问 PADD 维度：新增 `scripts/eia-padd3-country-crude-imports-excel.mjs` 和 `scripts/eia-all-padds-country-crude-imports-excel.mjs`，使用 EIA `/petroleum/move/impcp/data/` 月度正式数据，process `IP0`。全 PADD 输出为 `reports/eia/all-padds-crude-imports-canada-venezuela-2023-08_to_2026-08.xlsx`。
- 发现并修正 first weekly workbook bug：EIA `wimpc` 对 Venezuela 早期周度返回 `value: null`，旧脚本用 `Number(null)` 误转成 0。已改为保留空白；第一条非空周度为 2024-09-27（297 kb/d）。PADD 月度 `impcp` 2023-08 起有委内瑞拉正式月度数据，两者口径不同。
- Tianshu 要求按同样国家/三年条件进一步区分 crude grade。确认 EIA `wimpc`/`impcp` 只有 product/process/duoarea/series，不含 grade；改用 EIA Company Level Imports / Crude Import Tracking Tool 数据。新增 `scripts/eia-company-crude-imports-by-grade-excel.mjs`，从 companylevel archive 下载 2023/2024 年度文件和 2025/2026 月度文件，过滤 `PROD_CODE=025`、Canada/Venezuela，并用 APIGRAVITY + SULFUR 派生 gradeId/gradeName（LSW/LSO/MED/HSW/HSO）。输出 `reports/eia/company-crude-imports-canada-venezuela-by-grade-2023-08_to_2026-05.xlsx`；当前官方最新月度到 2026-05。
- Tianshu 提供 EIA `/v2/crude-oil-imports/data/` query。确认该 endpoint 原生支持 `gradeId`，数据源同为 EIA-814/company-level imports，适合做干净汇总；Canada 用 `CTY_CA` 比 `REG_CA` 语义更清楚（二者在 2023-08 至 2026-05 US total 总量相同）。新增 `scripts/eia-crude-oil-imports-api-grade-summary-excel.mjs`，分别拉 `destinationType=US`、`RP`、`PP`，避免 Port PADD 和 Refinery PADD 混加双算。输出 `reports/eia/crude-oil-imports-api-canada-venezuela-grade-summary-2023-08_to_2026-05.xlsx`，包含 US/PADD long+wide 汇总和 period totals。
- Tianshu 要求过去三年按 PADD 的炼厂加工量和开工率。新增 `scripts/eia-padd-refinery-runs-utilization-excel.mjs`，使用 EIA `/v2/petroleum/pnp/wiup/data/` Weekly Inputs & Utilization。拉取 `YIY/EPC0` crude oil refinery net input、`YIY/EPXXX2` gross inputs、`YRL/(NA)` operable crude distillation capacity、`YUP/(NA)` utilization。PADD 1-5 使用官方 YUP，NUS 因 wiup 无 YUP series，用 gross inputs / operable capacity 派生。输出 `reports/eia/eia-padd-refinery-runs-utilization-2023-08-19_to_2026-08-19.xlsx`；EIA 当前最新周度为 2026-08-07。
