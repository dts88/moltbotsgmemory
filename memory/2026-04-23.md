## 2026-04-23

- Tianshu 明确要求把 `MEMORY.md` 收敛为“长期稳定记忆”，不要继续堆静态 cron 清单、阶段性研究笔记、按时间整理的系统调整记录。
- 关于 cron 的长期原则已确定：真实性、启用状态与审计都应以 live `cron.list(includeDisabled:true)` 为准，而不是依赖 MEMORY 里的白名单或任务列表。
- Platts refined products / Mogas / GO / Jet / MTBE 的方法论细节应放进对应 skills；Dubai cracking margin / DBSCM00 的研究、符号表、验证结果也应放进对应 skill references，而不是主 `MEMORY.md`。
- EIA 的已修复实现坑（`fetchCountryImports()` 拉取长度过小）应保存在 `skills/eia-data/`，不再占用 `MEMORY.md`。
- Telegram 相关长期信息应合并存放，避免 `Telegram Topics` 与 `Telegram 配置` 两处重复。
- OilClaw 商业化路线当前视为不继续推进；相关商务/Onboarding 内容已归档到 `memory/2026-04-23-oilclaw-archive.md`，平时不再保留在 `MEMORY.md` 主体。
