# 2026-07-23

- 02:42 UTC: Tianshu asked why `Crude Daily Price Snippet to WhatsApp` failed and why it did not use the system default model. Checked live cron `571fb419-b6a9-4542-91f6-9550bbceab09`: repeated failures were DeepSeek Flash/Pro 60s idle timeouts, then an automatic retry succeeded. Removed the per-job `payload.model` override with `openclaw cron edit ... --clear-model`; verified `payloadModel: null`, so the job now follows normal/default cron model precedence. Schedule/delivery/timeout unchanged.
