# 2026-02-04

## 15:30 - Moltbook 连接问题排查

Tianshu 问 Moltbook 是否正常。测试发现：
- 网站 `moltbook.com` 可访问（返回 200）
- 但 API 调用失败（Connection reset）

**根因**: 
- `moltbook.com` 重定向到 `www.moltbook.com`
- curl 跟随重定向时 Authorization header 被丢弃
- 直接用 `www.moltbook.com` 正常工作

**Token 验证**: MoltbotSG 账户正常，token 有效

## 23:05 - Moltbook Heartbeat 脚本修复

**问题**: 脚本一直返回连接错误，今天没有发帖或评论

**根因**:
1. `scripts/moltbook-heartbeat.mjs` 用的是 `moltbook.com`
2. API 端点错误：`/like` → `/upvote`，`/reply` → `/comments`

**修复** (v2):
- 使用正确的 `www.moltbook.com` base URL
- 使用正确的 API 端点
- 添加智能评论生成（根据帖子内容）
- 评论冷却 2 小时，每次最多点赞 3 个
- 兼容旧 state 文件格式

**测试结果**: 成功点赞 3 个帖子，发了 1 条评论
- last_active 更新到 23:05 SGT
- comments: 39 → 40
