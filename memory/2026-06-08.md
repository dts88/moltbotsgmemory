# 2026-06-08

## 16:45 - 旧知识库废弃

- Tianshu 表示要重新考虑创建知识库，并要求先彻底删除和弃用之前的知识库。
- 已确认 live cron 中没有知识库导入/向量化任务。
- 已将旧知识库/RAG 产物移入可恢复 trash：`.trash/deprecated-knowledge-base-20260608-1645-sgt/`
  - `.rag-index.json`
  - `reports/knowledge-base.json`
  - `reports/vectorized-sources.json`
  - `reports/knowledge/`
  - `scripts/vectorize.mjs`
  - `scripts/import-reports.mjs`
  - `scripts/extract-cnpc-kb.mjs`
  - `scripts/md-to-docx.mjs`
  - `scripts/rag/`
- 旧升级备份里同名的知识库脚本副本也已移入同一个 trash 目录，避免后续误用。
- 已更新 `reports/index.json`，移除 `knowledge` 分类和对应条目，并标记旧知识库已废弃。
- 已更新 `MEMORY.md`：删除旧知识库入口和 `reports/vectors.json` 大文件待处理项，保留报告目录 `reports/` 作为普通市场资料目录。

## 17:17 - 新石油知识库骨架

- Tianshu 确认按新的知识库理解推进：横向/纵向存储石油行业数据与分析，保留机构口径差异，按时间尺度加权，并持续吸收高盛、Citadel 等顶尖机构的方法论。
- 已创建 `knowledge/`：
  - `knowledge/README.md`：总体架构与运行原则
  - `knowledge/config/retrieval-policy.json`：quick / standard / deep / audit 检索预算、时间衰减、排序权重
  - `knowledge/schema/v1/`：document、card、observation、timeseries-point、forecast、playbook schema
  - `knowledge/data/curated|raw|runtime/`：curated 小数据、raw 大文件、runtime 索引缓存目录
- 已创建 `skills/oil-knowledge-base/`，作为之后查询、导入、更新方法论 playbook 的操作入口。
- 已创建 `scripts/oil-kb.mjs`，支持 `status`、`init`、`policy <mode>`、`validate`。
- 已验证 JSON policy/schema 和 CLI status。Python/PyYAML 不可用，skill frontmatter 未用官方 yaml 脚本校验，但格式很简单。

## 17:29 - 新知识库断点续传机制

- Tianshu 强调新知识库运行可能遇到模型 token 上限或对话容量上限，必须能断点续传。
- 已为 `scripts/oil-kb.mjs` 增加 job/checkpoint 命令：
  - `job-create <type> <name>`
  - `job-list`
  - `job-status <job-id>`
  - `job-step <job-id> <phase> <status> [note]`
  - `job-item <job-id> <completed|failed|skipped> <item-id> [note]`
  - `job-artifact <job-id> <artifact>`
- 已新增 `knowledge/schema/v1/job.schema.json`。
- 已更新 `knowledge/README.md`、`skills/oil-knowledge-base/SKILL.md`、`skills/oil-knowledge-base/references/operating-model.md`，明确长任务必须写 checkpoint，聊天上下文不是 durable state。
- 已创建第一个可续传任务：
  - Job ID: `20260608093131-oil-kb-ingestion-pipeline-pilot`
  - 状态文件: `knowledge/data/runtime/jobs/20260608093131-oil-kb-ingestion-pipeline-pilot/job.json`
  - 当前阶段: `planning`
  - 下一步: 根据 `knowledge/data/curated/ingestion-pipeline-plan.json` 选择第一条 pipeline，建议 EIA weekly inventory 或 Platts MOC/heards。
- 已验证 `node scripts/oil-kb.mjs validate`、`status`、`job-list` 正常。

## 17:35 - EIA weekly inventory 样板导入

- 用户要求继续推进新知识库。
- 使用 `oil-knowledge-base` skill 规则，从 checkpoint 任务 `20260608093131-oil-kb-ingestion-pipeline-pilot` 继续。
- 选择 EIA weekly inventory 作为第一条 ingestion pipeline 样板，原因：结构化程度高，适合验证 document/card/observation/timeseries/provenance。
- 新增 `scripts/oil-kb-ingest-eia-weekly.mjs`：
  - 调用 `scripts/eia-weekly-report.mjs json`
  - 写入 raw payload: `knowledge/data/raw/eia/eia-wpsr-weekly-2026-05-29.json`
  - upsert curated JSONL:
    - `knowledge/data/curated/documents.jsonl`
    - `knowledge/data/curated/cards.jsonl`
    - `knowledge/data/curated/observations.jsonl`
    - `knowledge/data/curated/timeseries-points.jsonl`
  - 支持 `--job=<job-id>` 自动更新 checkpoint。
- 已导入 EIA week ending `2026-05-29`：
  - 1 document
  - 1 card
  - 15 observations
  - 15 timeseries points
- 已增强 `scripts/oil-kb.mjs validate`：除 schema/policy 外，也检查 curated JSONL 是否可解析。
- 已新增 `scripts/oil-kb.mjs search "<query>" [mode]`，先查 cards/curated evidence，不打开 raw source；quick 模式不会返回 evidence snippets。
- 验证通过：
  - `node scripts/oil-kb.mjs validate`
  - `node -c scripts/oil-kb.mjs`
  - `node -c scripts/oil-kb-ingest-eia-weekly.mjs`
  - `node scripts/oil-kb.mjs search "EIA crude inventory" quick`
- Job `20260608093131-oil-kb-ingestion-pipeline-pilot` 已完成，`nextAction` 为：Job completed. No resume action required unless adding a new phase or rerunning a batch.

## 17:42 - Platts MOC/heards 样板导入

- 用户要求继续进行知识库构建。
- 读取 `oil-knowledge-base` skill 和 checkpoint job 列表后，创建第二个可续传任务：
  - Job ID: `20260608094228-platts-moc-heards-pilot`
  - 目标：Platts MOC/heards 高时效市场观察 pipeline。
- 为避免重复消耗 Platts API/token，使用现有 monitor cache：`.cache/platts-monitor/latest.json`。
- 新增 `scripts/oil-kb-ingest-platts-monitor.mjs`：
  - 读取 Platts monitor cache
  - 解析 Top News / Market Stories / Heards 的编号条目和 sourceId
  - 写入 raw payload: `knowledge/data/raw/platts/platts-monitor-2026-06-08-20260608093448.json`
  - upsert curated JSONL:
    - `knowledge/data/curated/documents.jsonl`
    - `knowledge/data/curated/cards.jsonl`
    - `knowledge/data/curated/observations.jsonl`
  - 支持 `--job=<job-id>` 自动更新 checkpoint。
- 已导入 Platts monitor digest `2026-06-08`：
  - 1 document
  - 1 card
  - 12 observations
  - 每条 observation 保留 sourceId / Platts Connect URL provenance。
- 当前 curated 数据规模：
  - cards: 2
  - documents: 2
  - observations: 27
  - timeseries-points: 15
- 验证通过：
  - `node scripts/oil-kb.mjs validate`
  - `node -c scripts/oil-kb-ingest-platts-monitor.mjs`
  - `node scripts/oil-kb.mjs search "Platts Dubai MOC" quick`
  - `node scripts/oil-kb.mjs search "Platts Dubai MOC" standard`
- Job `20260608094228-platts-moc-heards-pilot` 已完成，全部阶段完成：planning、source_selection、document_registration、card_generation、structured_extraction、conflict_check、validation。
