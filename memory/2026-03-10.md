# 2026-03-10 (周二)

## Dubai MOC Report 完善

### 问题与修复
- 格式漂移：每次 cron 输出格式不一致
- 解决：在 cron payload 锁定格式模板，写入 MEMORY.md，创建技能

### 黄金模板
- **3月9日输出** = 标准参考
- 格式：标题→Assessment/Premium→Partials→Declarations→Cash窗口→市场观察→数据源
- 章节顺序锁定，不可增减，修改需 Tianshu 批准

### 关键教训
- FluxOfficials 少计 declarations（3/10报6个，实际8个）→ 以 Platts heards 逐条为准
- Platts heards API 只保留当天，次日清空 → cron 必须实时存档到 `reports/moc-daily/YYYY-MM-DD.json`
- Cash Oman 有时不发布（Oman partials 无成交时）→ 标"数据待更新"
- MOC 相关沟通主要走 **Telegram MOC topic** (-1003727952836, threadId: 2)

### 新增文件
- `skills/dubai-moc-report/SKILL.md` — 完整技能
- `reports/moc-daily/` — 存档目录（已创建）

## Cron 调整

### Polymarket + Hormuz 合并
- Hormuz Monitor (4h) 禁用
- Polymarket Geopolitical Monitor: 1h → 4h，模型改 Sonnet
- 合并后：7个地缘市场 + 霍尔木兹专题在同一条消息

## 全局模型调整
- 全局默认: Opus → **Sonnet**（降低 token 消耗）
- 例外：Tianshu 主 session (WhatsApp DM) 保持 Opus
- 例外：Dubai MOC cron 显式 Opus
- 新任务默认 Sonnet，Tianshu 特别说明才用 Opus

## OpenClaw Compact 说明
- OpenClaw 没有 `/compact` 命令
- 等效操作：发送 `/new` 开启新 session（丢弃上下文）
- 自动压缩：config 中 `compaction.mode = "safeguard"` 在 context 满时自动处理

## Session 模型架构确认 (2026-03-10 晚)
- Tianshu Telegram DM = 与 WhatsApp DM **同一 session**（identityLinks 合并）
- Telegram 群组 topic（如"商业化"）= **独立 session**，用全局默认 Sonnet
- 如需某 topic 用 Opus，需在该 topic 发消息后手动设 session override
- 注意：config.patch 重启后当前 session 显示 sonnet-4-6，Tianshu 主 session 未单独设 Opus override
  → 待下次会话时如需恢复 Opus，Tianshu 可发 `/model opus` 或告知我设置
